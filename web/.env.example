# chain-sync-watch web frontend
# Copy to .env.local and tune for your environment.

# Base URL for the csw-server HTTP API. The client prepends this to
# every request. Leave unset in production if the frontend is served
# behind a reverse proxy that forwards /api to csw-server.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
