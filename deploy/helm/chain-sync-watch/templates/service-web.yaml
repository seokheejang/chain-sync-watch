{{- if .Values.web.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "chain-sync-watch.fullname" . }}-web
  labels:
    {{- include "chain-sync-watch.componentLabels" (merge (dict "component" "web") .) | nindent 4 }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chain-sync-watch.componentSelectorLabels" (merge (dict "component" "web") .) | nindent 4 }}
{{- end }}
